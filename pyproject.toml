[project]
name = "debloater"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "confkit[msgspec,pydantic]>=2.0.0",
    "herogold>=3.6.0",
]

[tool.uv.workspace]
members = [
    "args",
]
